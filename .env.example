OPENROUTER_API_KEY=
PORT=3000
WEB_ORIGIN=http://localhost:5173
# UI: origin the browser uses to reach the API (required in Docker).
# Local Vite can leave this empty and use the /api proxy.
# API_URL=http://localhost:3000
# VITE_API_URL=
# Optional. Docker Compose sets http://libsql:8080
# LIBSQL_URL=file:./data/quiz.db
# LIBSQL_AUTH_TOKEN=

